{
  "name": "json-resume",
  "version": "1.0.0",
  "description": "meryem iris resume",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "resume serve --theme eloquent-mod --resume resume.json",
    "export": "resume export resume.html --format html --theme even"
  },
  "author": "Meryem Iris",
  "license": "ISC",
  "dependencies": {
    "jsonresume-theme-eloquent-mod": "^4.24.0",
    "jsonresume-theme-even": "^0.22.0",
    "puppeteer": "^21.5.2",
    "resumed": "^3.0.1"
  }
}
