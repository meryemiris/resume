{
  "name": "json-resume",
  "version": "1.0.0",
  "description": "meryem iris resume",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "resumed serve --theme even --resume resume.json",
    "export": "resumed render --output resume.pdf --theme ./node_modules/jsonresume-theme-even/index.js"
  },
  "author": "Meryem Iris",
  "license": "ISC",
  "dependencies": {
    "jsonresume-theme-even": "^0.22.0",
    "puppeteer": "^21.5.2",
    "resumed": "^3.0.1"
  }
}
